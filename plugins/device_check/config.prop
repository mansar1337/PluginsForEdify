# Allowed device codenames (comma-separated, no spaces)
# Leave empty to skip check
ALLOWED_DEVICES=cepheus,raphael,davinci

# What to do on mismatch:
#   abort — exit 1 (stops install when used with assert() in updater-script)
#   warn  — print warning and continue
ON_MISMATCH=abort
